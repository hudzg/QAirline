package com.QAirline.repository;

import com.QAirline.model.LegInstance;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LegInstanceRepository extends JpaRepository<LegInstance, Long> {
}
